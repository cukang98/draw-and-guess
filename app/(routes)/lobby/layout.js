export const metadata = {
  title: 'Lobby',
  description: 'Join or create a game room to start drawing and guessing with friends!',
};

export default function LobbyLayout({ children }) {
  return children;
}
