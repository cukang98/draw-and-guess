export const metadata = {
  title: 'Game Room',
  description: 'Currently playing Draw & Guess! Show off your artistic skills or guess what others are drawing.',
};

export default function GameLayout({ children }) {
  return children;
}
