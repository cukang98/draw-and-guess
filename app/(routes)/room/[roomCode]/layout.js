export const metadata = {
  title: 'Waiting Room',
  description: 'Waiting for players to join the Draw & Guess room. Share the code to invite your friends!',
};

export default function RoomLayout({ children }) {
  return children;
}
